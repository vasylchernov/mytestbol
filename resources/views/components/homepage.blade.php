<div>
{{--    @foreach($products as $p)--}}
{{--        {{ $p->name }}--}}
{{--    @endforeach--}}


    <br>
    @foreach($p2 as $p)
        {{ $p }}
    @endforeach
    <br>
    @foreach($products as $p)
        {{ $p }}
    @endforeach
</div>
